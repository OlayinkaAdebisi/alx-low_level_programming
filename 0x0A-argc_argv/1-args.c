#include <stdio.h>
#include <stdlib.h>
/**
 * main - index
 * @argc: counts
 * @argv: array
 * Return: (Success)
 */
int main(int argc, char __attribute__((unused)) *argv[])
{
	int i, n;

	for (i = 0; i < argc; i++)
	{
		n = i;
	}
	printf("%d\n", n);
	return (0);
}
