#include <stdio.h>
/**
 * main - Entry point
 * Description: 'fizz buzz'
 * Return: Always 0 (Success)
 */
int main(void)
{
	int num;

	for (num = 1; num <= 9; num++)
	{
		if (num = 1)
		{
			putchar(num);
		}
		else if (num % 3 != 0)
		{
			printf("fizz");
		}
	}
}
